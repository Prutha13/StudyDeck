# StudyDeck Client Environment Variables Example
# Set to your hosted backend API URL in production (e.g. on Render, Railway, Fly.io)
# In local development, leave unset or empty to use Vite's local dev proxy (/api -> localhost:5000)
VITE_API_URL=https://your-api-domain.com/api

